{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run StreamDeckApp",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/streamdeck",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build",
            "MIMode": "lldb"
        }
    ]
}
